# Versiones fijadas: un esqueleto que no se puede reconstruir igual dentro de
# seis meses no es reproducible, y la reproducibilidad es la mitad del punto
# de la Fase 0.

fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
psycopg[binary]==3.3.4

# Criterio F: migraciones desde la Fase 1 (CLAUDE.md §4). El esquema deja de
# crearse con `create_all`; dos mecanismos a la vez es una fuente de deriva.
alembic==1.19.1
pydantic-settings==2.15.0

# Criterio B4: Argon2id. Es la implementacion de referencia y trae los
# parametros recomendados por defecto, que es justo lo que no conviene
# inventarse a mano.
argon2-cffi==25.1.0

# El exportador escribe con ella el frontmatter del vault y el respaldo lo lee.
# Llegaba solo de forma transitiva, por `uvicorn[standard]`: si una versión de
# uvicorn dejara de arrastrarla, la API no arrancaría.
pyyaml==6.0.3

# Criterio R1: las miniaturas de las imágenes de la carpeta de la pieza, la
# única excepción del §2.2. Dependencia anunciada en el alcance de la Fase 3.
# Sus wheels traen libjpeg y libwebp: la imagen no necesita paquetes del
# sistema.
pillow==12.3.0

# `docker compose run api pytest` está documentado en CLAUDE.md §5, así que
# las pruebas corren dentro de esta misma imagen y sus dependencias viven aquí.
pytest==9.1.1
httpx==0.28.1
