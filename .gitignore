# entorno
.env
.env.local
!.env.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.db
*.sqlite3

# node
node_modules/
dist/
.vite/
*.tsbuildinfo

# credenciales de plataformas — NUNCA versionar
tokens/
*.token.json
client_secret*.json

# material de producción — va por Syncthing, no por git (ADR 0003)
media/
miniaturas/

# sistema
.DS_Store
Thumbs.db

# Claude Code — config local por desarrollador, no compartida
.claude/settings.local.json

# punto de montaje por defecto del vault; el real se configura en .env
vault/
