# Astrolabio — plantilla de configuración.
# Copiar a .env y rellenar. NUNCA poner valores reales en este archivo.

# --- base de datos ---
POSTGRES_USER=astrolabio
POSTGRES_PASSWORD=
POSTGRES_DB=astrolabio
DATABASE_URL=postgresql+psycopg://astrolabio:@db:5432/astrolabio

# --- api ---
# Clave de firma de sesión. Generar con: python -c "import secrets; print(secrets.token_urlsafe(48))"
SESSION_SECRET=
# true solo cuando se sirva por HTTPS (variante C del ADR 0002)
COOKIE_SECURE=false

# --- siembra inicial (solo para el primer arranque) ---
SEED_INVESTIGADOR_USER=johan
SEED_INVESTIGADOR_PASSWORD=
SEED_EDITOR_USER=
SEED_EDITOR_PASSWORD=

# --- frontend ---
# Vacío = mismo origen. Se rellena solo si web y api se sirven por separado.
VITE_API_BASE=

# --- rutas locales (ADR 0003) ---
# Carpeta Syncthing con el material de producción. No se versiona.
MEDIA_ROOT=./media
# Carpeta del vault donde se exportan las notas de contenido (ADR 0001).
VAULT_CONTENIDO_PATH=
